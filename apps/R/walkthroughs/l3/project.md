# Create a standard R project

A `.Rproj` file plus `.Rprofile` gives you a consistent working directory and
encoding defaults, the same convenience RStudio users rely on.

Run **R: Create Project** and choose the **L1 - Standard project** tier.
