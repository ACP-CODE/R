# Write your first script

Console lines disappear when you close the terminal. Save your work in a `.R` file instead,
then run it with `Rscript main.R` or line-by-line from the editor.

Use **R: Create Project** to scaffold a starting file.
