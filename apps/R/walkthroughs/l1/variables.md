# Variables and vectors

Assign a value with `<-`, and build your first vector with `c()`:

```r
x <- 42
nums <- c(1, 2, 3)
nums * 2
```

R is vectorized: `nums * 2` multiplies every element at once, no loop required.
